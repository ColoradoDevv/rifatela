# URL base de la API (solo aplica si usas un bundler con env, ej. Vite)
# En proyectos sin bundler, define window.APP_API_BASE en el HTML antes de cargar los scripts.
# VITE_API_BASE=/api
# VITE_API_BASE=https://api.tudominio.com
