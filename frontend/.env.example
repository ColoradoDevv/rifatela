# Vercel environment variable (recommended)
# Used by frontend/api/[...path].js to proxy /api/* requests to Railway.
RAILWAY_API_BASE_URL=https://your-railway-backend.up.railway.app/api

# Optional direct client override if you do NOT use the /api proxy:
# window.APP_API_BASE = 'https://your-railway-backend.up.railway.app/api'

# Optional for bundlers such as Vite:
# VITE_API_BASE=https://your-railway-backend.up.railway.app/api
