# Base de datos
MONGO_URI=mongodb://localhost:27017/rifatela

# Obligatorio: secreto para firmar JWT (usa un valor aleatorio largo en producción)
JWT_SECRET=genera-un-secreto-aleatorio-seguro-aqui

# Opcional: orígenes CORS permitidos (producción). Ej: https://tudominio.com
# CORS_ORIGIN=https://tudominio.com

# Opcional: entorno (development | production)
# NODE_ENV=development

# Opcional: puerto del servidor
# PORT=5000
